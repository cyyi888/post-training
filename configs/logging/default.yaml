level: INFO
to_console: true
to_file: true
filename: run.log
format: "%(asctime)s | %(levelname)-7s | %(name)s | %(message)s"
datefmt: "%Y-%m-%d %H:%M:%S"
# 同时把 Hydra/transformers 噪声压到 WARNING
quiet_libs:
  - hydra
  - datasets
  - urllib3
  - httpx
  - filelock
